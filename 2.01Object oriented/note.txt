类：有相同属性和行为的事物的集合
对象：从类中具象化出一个具体的有实物的东西
	实例化：
		类 名字 = new 类();
		Person zhangsan = new Person();

属性：事物本身所具有的特性
方法：事物所具有的行为
	有返回值的方法：
		一个方法里如果有返回值，就一定要用return
		public int add(){
			int a = 10 + 10;
			return a;
		}
		



